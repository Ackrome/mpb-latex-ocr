paths:
  manifest: data/mathwriting/manifest.csv
  image_root: null
  output_dir: outputs/mathwriting_deep_cnn
  tokenizer_path: outputs/mathwriting_deep_cnn/tokenizer.json

data:
  image_height: 160
  image_width: 640
  batch_size: 12
  num_workers: 2
  max_label_length: 256
  augment: true
  augmentation_profile: handwriting
  augmentation_strength: 1.0

mlflow:
  experiment_name: latex-ocr-mathwriting
