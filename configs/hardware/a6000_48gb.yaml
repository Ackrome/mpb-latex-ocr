data:
  batch_size: 64
  num_workers: 8

trainer:
  precision: bf16-mixed
  accumulate_grad_batches: 1
  gradient_clip_val: 1.0

model:
  d_model: 384
  decoder_layers: 6
  nhead: 8
  dim_feedforward: 1536
